
<!-- Project Details -->

<div class='project-title-container'>
	<h2 class='project-show-title'><%= @project.title %></h2>
	<h4 class='project-show-creator'>by <%= link_to @project.user.name, @project.user %></h4>
</div>

<%= render 'projects/project_links' %>

<div class='project-show-container'>
	<div class='large-9 columns'>
		<div class='project-container'>
			<%= image_tag @project.image.url %>
			<p>Description: <%= raw(@project.description) %></p>
		</div>
	</div>
	<div class='large-3 columns'>
		<div class='info-container'>
			<p><%= @project.pledges.count %> backers</p>
			<p><%= number_to_currency(@project.funded_amount / 100) %> pledged of <%= number_to_currency(@project.goal / 100) %> goal</p>
			<p><%= @days_left %> days left </p>
		</div>
		<div class='reward-container'>
			<h3>Rewards</h3>
			<% @rewards.each do |reward| %>
				<div class='reward'>
					<p>Backers: <%= reward.pledges.count %></p>
					<p><%= number_to_currency(reward.amount / 100) %></p>
					<p><%= reward.description %></p>

					<p><%= link_to "View Reward", project_reward_path(@project, reward), class: 'button tiny' %></p>
				</div>
			<% end %>
		</div>
	</div>
	<br>
	<br>

	<br>
	<br>

<!-- Project Comments -->

	<div id="comments">
		<%= render @comments  %>
	</div>


	<h2>New Comment</h2>

	<%= form_for [@project, @comment], remote: true do |f| %>
	  <p>
	    <%= f.label :body %><br />
	    <%= f.text_area :body %>
	  </p>
	  <p><%= f.submit "Submit" %></p>
	<% end %>
</div>


	<br>
	<br>

	<!-- MapBox -->


	<div data-lat="<%= @project.latitude %>" data-long = "<%= @project.longitude %>" data-title="<%= @project.title %>" data-description="<%= @project.description %>" id='map'>

	</div>

 <style type="text/css">

		#map {
			width: 90%;
			height: 400px;
			margin: auto;
		}

 </style>

<script>

	initMapDisplay();

</script>
