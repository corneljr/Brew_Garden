<%= form_for [@project, @update] do |f| %>
	<%= f.text_field :title, placeholder: 'title' %>
	<%= f.text_area :body, placeholder: 'update', class: 'ckeditor' %>
	<%= f.submit 'Post', class: 'button tiny' %>
<% end %>